import { type NextPage } from 'next';
import Layout from '~/components/Layout';

const Post: NextPage = () => {
    return (
        <Layout>
            <div className='flex items-center border-[1px] border-t-0 border-solid border-gray-200'></div>
        </Layout>
    );
};

export default Post;
